version: "3.4"
services:
    csharp:
        build:
            target: build
        working_dir: /work
        stdin_open: true
        volumes:
            - ./csharp/src:/work
    # nodejs:
    #     build:
    #         target: build
    #     working_dir: /work
    #     entrypoint: /bin/sh
    #     stdin_open: true
    #     tty: true
    # python:
    #     build:
    #         target: build
    #     working_dir: /work
    #     entrypoint: /bin/sh
    #     stdin_open: true
    #     tty: true
 